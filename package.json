{
  "name": "feedback-ops-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Internal product-operations dashboard for triaging customer feedback into a prioritized backlog.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}
